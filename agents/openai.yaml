interface:
  display_name: "HarmonyOS AppGallery 发布"
  short_description: "安全完成 HarmonyOS 签名、验包、上架与审核提交"
  default_prompt: "Use $publishing-harmonyos-appgallery-skill to prepare and verify a HarmonyOS AppGallery release through the final submission gate."
policy:
  allow_implicit_invocation: true
