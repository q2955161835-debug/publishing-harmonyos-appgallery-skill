# AppGallery 审核驳回整改流程任务验收

## 汇总

| 项目 | 内容 |
| --- | --- |
| 涉及模块 | Skill 触发与路由、发布流程、字段清单、故障排查、文档契约 |
| 验收等级 | L3（分发与外部提交门禁） |
| 目标 | 将审核报告绑定到被审核软件包，形成可验证的整改与重新提交闭环 |
| 验证结果 | 自动验证通过；首次独立验收发现 P1 后已修复，等待复验 |
| 问题风险 | 当前无项目级 `AGENTS.md`；真实门户字段仍需执行时读取 |
| 最终结论 | 未执行 |

## 详细报告

### 文档契约

- 验证步骤：运行 `pwsh -NoProfile -File .\scripts\test-skill-documentation.ps1`。
- 预期结果：审核驳回触发、报告身份、被审核软件包不可变证据、未知身份 fail-closed、问题矩阵、名称/图标、一句话简介、compatible/target API、审核可达性、已选包回读和重提授权契约全部存在。

### 文档与脚本质量

- 验证步骤：运行 `git diff --check`，并使用 PowerShell Parser 解析 `scripts/*.ps1`。
- 预期结果：无空白错误；全部脚本解析无错误。

### 场景验收

- 验证步骤：独立子 Agent 使用本 Skill 处理包含“审核仍使用旧包、名称不一致、最低适配过高、功能场景有限”的失败报告，只提供原始文件和任务，不提供预期答案。
- 预期结果：先绑定报告与失败版本已选包，再建立逐项整改矩阵；不把本地候选当作审核包，不用门户文案或自测视频替代包/功能修复，并在重新提交前停在新的授权门禁。

### 最终处理

- 验证步骤：复核 Git 差异、完整验证输出和独立验收意见。
- 预期结果：结论为“通过”后，将本记录移动到 `doc/验收/归档/2026-08-11/`，再合并 `main` 并删除任务分支。
